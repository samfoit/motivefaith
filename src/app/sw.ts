/**
 * Service Worker source reference.
 *
 * The compiled service worker lives at `public/sw.js` and is registered
 * by the `useServiceWorker` hook in `src/lib/hooks/useServiceWorker.ts`.
 *
 * Features:
 * - Push notification display (title, body, icon, badge)
 * - Notification click → navigate to relevant page
 * - Immediate client claim on activate
 */

export {};
